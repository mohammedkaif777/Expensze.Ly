import { PagePlaceholder } from "@/components/page-placeholder";

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Expenses"
      description="The expenses page is under construction."
    />
  );
}