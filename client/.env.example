# Copy this file to .env.local when running the frontend locally.
# The Next.js dev server proxies /api/* requests to the backend via rewrites.

# Backend API target used by the Next.js rewrites proxy.
API_PROXY_TARGET=http://localhost:5000